# spring.application.name=todo

server:
  port: 8181

spring:
  application:
    name: todo
  datasource:
    url: jdbc:mysql://localhost:3306/jpa?serverTimezone=Asia/Seoul
    username: spring
    password: spring
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    database: mysql
  servlet:
    multipart:
      max-file-size: 10MB # 1 file max-size
      max-request-size: 20MB # total request max-size

# swagger setting
springdoc:
  swagger-ui:
    path: /myapi.html

jwt:
  secret: yMC2b/aXpVegk8W+xEfcLwO+LMrX7DpiSc8hk2U2X0K7QcDb5f0Bk4dpfCU5fH7PN2iXST4lB2KOjw9WXDdH+w==

# upload file path
upload:
  path: C:/todo_upload
